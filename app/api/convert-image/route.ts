import {NextRequest} from "next/server";import sharp from "sharp";export const runtime="nodejs";const allowed=new Set(["jpg","png","webp"]);
export async function POST(req:NextRequest){const f=await req.formData(),file=f.get("file"),format=String(f.get("format")||"webp"),max=Number(process.env.MAX_UPLOAD_MB||100)*1024*1024;if(!(file instanceof File)||!file.type.startsWith("image/"))return Response.json({error:"Please upload an image."},{status:415});if(!allowed.has(format)||file.size>max)return Response.json({error:"Unsupported format or file too large."},{status:400});try{let p=sharp(Buffer.from(await file.arrayBuffer())).rotate();if(format==="jpg")p=p.jpeg({quality:88});else if(format==="png")p=p.png({compressionLevel:8});else p=p.webp({quality:88});const out=await p.toBuffer();return new Response(out,{headers:{"Content-Type":format==="jpg"?"image/jpeg":`image/${format}`,"Content-Disposition":`attachment; filename="mediasave.${format}"`}})}catch{return Response.json({error:"Image conversion failed."},{status:500})}}