const data:Record<string,{title:string,body:string[]}>={ "convert-video-online":{title:"How to Convert a Video Online",body:["Choose a tool that accepts the source format you own.","Upload the file, select the desired output format and start processing.","Download the result and delete local copies you no longer need."]}, "compress-video":{title:"How to Compress Video Without Losing Too Much Quality",body:["Compression works by reducing bitrate, resolution, frame rate or using a more efficient codec.","Start with a moderate quality preset and compare the result before making it more aggressive.","Keep an original copy when the source is important."]}, "webp-vs-jpg":{title:"WEBP vs JPG: Which Image Format Should You Use?",body:["WEBP can provide smaller files while supporting modern web workflows.","JPG remains broadly compatible and is useful for photos.","For websites, choose based on browser support, workflow and desired file size."]}}; export function generateStaticParams(){return Object.keys(data).map(slug=>({slug}))} export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=data[slug]||data["convert-video-online"];return <article className="max-w-3xl mx-auto px-4 py-14"><h1 className="text-4xl font-black">{p.title}</h1>{p.body.map(x=><p key={x} className="mt-6 text-slate-300 leading-8">{x}</p>)}</article>}